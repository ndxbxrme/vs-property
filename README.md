# vs-property
```bash
git clone https://github.com/ndxbxrme/vs-property
cd vs-property
npm install -g grunt-cli
npm install
bower install
set REZI_ID={{yourClientID}}
set REZI_SECRET={{yourSecret}}
grunt
```
